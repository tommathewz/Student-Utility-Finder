# Use the official Nginx Alpine image as the base
FROM nginx:alpine

# Copy the static files to the Nginx default html directory
COPY . /usr/share/nginx/html

# Expose port 80
EXPOSE 80

# The default Nginx command starts the server automatically
